extends layout

block content
	p DNS list :

	each item in dnslist
		li= h1= item.name 
			h2= item.primaryIP
			h3= item.secondaryIP
			h4= item.isISP
			h5= item.updatedAt
			a(href="/destroy/"+item._id) Delete
			br
			a(href="/validate/"+item._id) Validate