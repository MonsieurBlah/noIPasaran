extend layout

block content
	div.title2
		a(href="http://noipasaran.eu01.aws.af.cm/")<font color="black">no</font><font color="red">IP</font><font color="black">asaran</font>	
	form(name="dnsform", action="/submit", method="post")
		input(name="ServerName", type="text", id="DNS_NAME_ID", style="width:420px", placeholder="Type the ISP or DNS provider here")
		br/
		input(name="primaryIP", type="text", id="PR_IP_ID", style="width:420px", placeholder="Type the primary IP here")
		br/
		input(name="secondaryIP", type="text", id="SEC_IP_ID", style="width:420px", placeholder="Type the primary IP here") 
		br/
		p Check this if this is an ISP 
		input(name="isISP", type="checkbox", id="IS_ISP_ID")
		br/
		input(type="submit", action="", value="Submit") 

	p TODO : make the submit to mongo + CSS