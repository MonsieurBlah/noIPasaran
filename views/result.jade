extend layout

block content
	div.title2
		a(href="http://noipasaran.eu01.aws.af.cm/")<font color="black">no</font><font color="red">IP</font><font color="black">asaran</font>	
	iframe(width="800", height="700", src="http://#{ip[0]}", sandbox="")
	iframe(width="800", height="700", src="http://#{url}", sandbox="")
	p The best IP address of #{url} is #{ip} (make CSS button)
	div.goToButton
		input(type="submit", value="Go to #{url}", onClick="location='http://#{ip[0]}'")
		form(name="proxyform", action="")
			input(type="checkbox", name="proxy", value="proxy")Use proxy